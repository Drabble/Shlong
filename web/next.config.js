/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  poweredByHeader: false,
  reactStrictMode: false,
  serverRuntimeConfig: {},
  publicRuntimeConfig: {},
};

module.exports = nextConfig;
